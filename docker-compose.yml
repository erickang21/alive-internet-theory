services:
  backend:
    build: ./backend
    ports:
      # Localhost only: the extension calls http://127.0.0.1:5000.
      - "127.0.0.1:5000:5000"
    env_file: backend/.env
    volumes:
      - backend-data:/data
    restart: unless-stopped

volumes:
  backend-data:
